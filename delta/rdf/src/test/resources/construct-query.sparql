prefix senscience: <http://senscience.ai>
prefix schema: <https://schema.org/>

CONSTRUCT {
  ?person 	        schema:name             ?name ;
                    schema:birthDate        ?birthDate ;
                    schema:description      ?description ;
} WHERE {
  ?person 	        schema:name             ?name ;
                    schema:birthDate        ?birthDate ;
                    senscience:stringProperty  ?description ;
}
